#include <iostream>
#include <vector>
#include <stack>

#include "csr.h"

using namespace std;

void dfs(Csr graph) {

    stack<int> forDfs;
    vector<int> traversal, visited(csr.numVertices, -1);
}
